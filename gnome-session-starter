#!/usr/bin/env python3
"""Thin launcher so the app can run from a .desktop file without -m."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from gnome_session_starter.__main__ import main

if __name__ == "__main__":
    sys.exit(main())
